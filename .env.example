# Optional: increases GitHub API rate limit for live runs
GITHUB_TOKEN=

# Per-request timeout in milliseconds (default: 10000)
GITHUB_TIMEOUT_MS=10000

# Safety cap; reaching it is an error, not successful truncation (default: 20)
GITHUB_MAX_PAGES=20
